# Portfolio redesign verification

## Roomba RGB accent — 2026-09-25

The floor-patrol Roomba now carries a thin three-segment cyan, pink, and green status ring on its top shell. It reuses the room's existing emissive materials and RGB animation path, so it remains a small equipment detail and freezes with the established reduced-motion behavior. The regenerated recovery GLB contains 1,102 mesh nodes using 476 shared definitions and remains outside the browser runtime path. Desktop browser inspection confirmed that the ring is legible in the settled overview without competing with the desk, wall fixtures, or destination markers. Browser console warnings/errors: none. Scene contract, JavaScript syntax, portfolio checks, and the final diff check passed.

## Portfolio improvement completion pass — 2026-09-25

All 13 items in `PORTFOLIO-IMPROVEMENTS.md` were implemented without replacing the established cyber-lab direction. The desktop entry is identity-led; the full supporting-work archive is reachable; WebGL/JavaScript failure paths promote the Portfolio Overview; the overview has a compact mobile menu and evidence-led desktop introduction; every selected case now includes an inspectable artifact or explicit evidence-availability disclosure; and the 3D room has a named eight-destination workspace map.

The delivery also adds canonical/share metadata, Person JSON-LD, a 1200×630 social preview, `robots.txt`, `sitemap.xml`, dated profile language, 48px mobile brand targets, documented content tiers, and a dependency-free portfolio checker. The checker enforces local references, local anchors, incoming links, required metadata, no remote runtime assets, no 3D module preload on the entry shell, and raw-byte budgets of 250 KB for the entry shell and 1 MB for the full scene runtime.

Rendered verification covered the desktop lab entry and settled room, map-to-project navigation, Projects dossier, Portfolio Overview, archive disclosure, scanner evidence disclosure, 390×844 mobile entry/overview/menu, and 320×568 narrow overview. The browser console had no warnings or errors. `python scripts/check-portfolio.py`, JavaScript syntax checks, the scene contract check, and `git diff --check` passed. The scanner screenshot's former upstream URL returns 404, so the case visibly records the unavailable source artifact instead of presenting invented replacement evidence. No deployment, external contact action, or form submission was performed.

## Inward-facing corner composition pass — 2026-09-23

The room no longer stages every secondary object toward the camera. The sofa moved against the far-left wall, rotates 90° to face the desk, and keeps its small table immediately beyond the forward arm. The aquarium moved against the far-right wall and rotates inward toward the workstation; a slight diagonal preserves the intent while letting the default overview reveal the living interior. The overview radius increased to 14 units and FOV to 47° so both corner arrangements remain legible without flattening their orientation.

The aquarium now has five visible cyan LED elements: hood, lower frame, two vertical front edges, and cabinet wash. Its modeled switch and the keyboard-accessible Controls-panel button toggle both the LED emission and aquarium point light with `aria-pressed` state. Browser verification observed the button change to `Aquarium LEDs / Off` with `aria-pressed="false"`, then restored it to on.

The regenerated recovery GLB contains 998 mesh nodes using 424 shared definitions and 64 materials at 2,938,432 bytes. Contract checks enforce inward aquarium/sofa rotation, five aquarium LED elements, three moving fish, and non-intersection with the rack, contact shelf, and side table. The rendered overview shows both corner zones and retains an open center floor path.

## Neon wall, panoramic window, and living aquarium pass — 2026-09-23

Generic framed wall graphics, the small window, and the loose blue floor Ethernet cable were removed. The monitor wall now has a 4.5-unit panoramic night window with a procedural skyline, lit building windows, stars, haze, crescent moon, steel frame, mullion, sill, and subtle reflective glass. It remains physically clear of the Security+ credential frame. The remaining decorative wall hierarchy is deliberate: modeled cyan `DFIR` tubing and a pink verification-shield outline with a separate cyan check. Both use high-output emissive materials, short-range point lights, and the existing thresholded bloom rather than painted rectangles.

The right foreground now contains a cabinet-built planted aquarium with transparent glass panels, a water surface, deep-blue backing, hood light, varied gravel, river rocks, branched driftwood, five groups of curved aquatic plants, and a rising bubble column. Three distinct fish use tapered lathed bodies, curved tail and side fins, dorsal fins, eyes, flank markings, and softly emissive colored materials. Their bounded paths, body direction, vertical drift, and independent tail movement animate only when normal motion is enabled. The aquarium stays clear of the CCDC rack and contact shelf.

A rendered desktop inspection confirmed that both neon signs read as practical fixtures, the large exterior view is visible around the monitor, the central floor is clear, and the aquarium is fully inside the overview crop with fish and planted detail visible. The recovery GLB contains 993 mesh nodes using 421 shared definitions and 64 materials at 2,917,608 bytes. `node --check scene.js` and `node scene.contract-check.mjs` pass, including explicit assertions for three animated fish, removed floor cable, and non-intersecting aquarium/window placement. No deployment, external navigation, download, or contact action was performed.

## Practical-light, bloom, and environmental interaction pass — 2026-09-23

The live benchmark and local room were compared again after entry at the same desktop browser size. The benchmark's defining image behavior is a near-black shell punctuated by high-energy practicals, not uniformly reduced exposure. The local scene now uses ACES Filmic at 1.04 exposure, fog from 13–29 units, .075 ambient intensity, .035 room emission, side-wall fills below .25, a 48-intensity desk spotlight, 7.2 cyan monitor spill, a 26-intensity evidence spot, a 16-intensity rack spot, and separate short-range shelf, credential, lounge, contact, and desk-strip pools. A one-third-resolution bright-pass and separable blur composites restrained bloom above a .56 threshold. It adds no dependency and leaves the pixel-ratio cap at 1.25.

The physical task lamp, library light switch, and monitor power control are raycast interactions. Each changes actual light intensity and relevant emissive materials; matching controls in the Controls panel preserve keyboard access and expose `aria-pressed`. All three toggles were exercised off and back on in the browser and reported the expected pressed states. The lounge is 18% wider and 8% taller/deeper, with its side table moved clear; the regenerated recovery GLB contains 835 mesh nodes using 321 shared definitions and 35 materials at 2,034,776 bytes.

A rendered 1068×790 desktop inspection confirmed localized warm/cool pools, deeper black separation, distinct credential and shelf illumination, and zero document overflow. The local scene initialized in 197 ms in the observed browser session, with one canvas and no browser warnings or errors. All eight stable hash routes opened the expected dossier title with `aria-hidden="false"`. `node --check scene.js` and `node scene.contract-check.mjs` pass. The browser viewport override did not change the runtime viewport in this session, so mobile behavior was not re-claimed from a screenshot; the existing CSS/mobile fallback source was unchanged. No deployment, external navigation, download action, or contact submission was performed.

## Benchmark-led spatial navigation pass — 2026-09-22

The requested `true-web-clone` skill was installed from `SkyNotSilent/true-web-clone` into the Codex skills directory and its WebGL guidance was applied with the existing browser runtime; no duplicate Playwright, browser, framework, or package dependency was added. Live inspection of `avagianos-dev.gr` found a single 179 KB inline module using Three.js 0.160 and OrbitControls, a fully procedural scene with no downloaded model or texture bundle in the observed state, nine project destinations plus a lamp, HTML pins projected from 3D anchors, throttled raycasting, a 4.6-second intro camera pullback, static-shadow warmup, a 1.5 DPR cap, continuous ambient prop animation, light/day controls, and a right-side project panel. The benchmark does not reframe the camera for project panels; Tate's existing destination-specific camera states were retained as the stronger portfolio behavior.

The local room now opens at the forensic monitor and pulls back to a tighter overview in 2.2 seconds. Eight real HTML buttons are projected from the same world-space coordinates as the in-scene locators. Their labels appear in two short, separated waves and then collapse to numbered targets; hover, focus, and selection restore the label. This replaces the prior nearly invisible keyboard-only list without adding permanent card chrome to the room. The project dossier now has dialog semantics, makes the HUD and spatial controls inert while open, removes the canvas from the tab order, and restores the room state and canvas focus on close.

Rendered browser checks used 1280×720 desktop and 390×844 mobile. Local WebGL startup was observed at 129–134 ms with no console warnings or errors. All eight projected destinations opened the correct title, camera status, pressed state, and modal dossier; every open state made HUD/navigation inert, and every close restored `OVERVIEW`, removed all pressed states, returned canvas `tabIndex` to 0, and removed inert state. The mobile breakpoint hid WebGL, displayed the poster-led index, and exposed direct project, experience, CCDC, résumé, contact, and classic-portfolio links. No deployment, external navigation, download action, or contact-form submission was performed.

## Vibrant room and right-side spacing pass — 2026-09-22

The evidence locker, loose manual stack, foreground evidence case, open floor kit, and generic wall clock were removed from the right side. The technical library moved outward and forward to become the single side anchor, leaving a clear floor path and a cleaner desk silhouette. Its books use a dedicated saturated but non-emissive palette so they read as colored objects rather than light sources.

Two authored neon wall marks now frame the workspace: a cyan verification shield and a magenta security lock. Their geometry and short-range practical lights provide the reference-inspired neon character while keeping the marks cybersecurity-specific and limiting glow to two intentional zones. The wider room palette now uses more saturated blue, orange, green, plant, brass, screen, and wood values; the continuous blue desk strip remains the primary horizontal lighting cue.

A rendered 1280×720 inspection confirmed both neon marks are unobstructed, the library is fully legible, the cabinet and loose right-side equipment are absent, the foreground remains open, and all eight locators remain visible. The scene recorded a 97 ms local boot with zero page overflow and no browser warnings or errors. The regenerated recovery GLB contains 476 mesh nodes using 258 shared definitions and 26 materials at 1,102,168 bytes. Static validation passes all eight destinations and ten HTML routes; no deployment or form submission was performed.

## Camera, blue-light, and cyber-wall refinement — 2026-09-22

Horizontal and vertical drag signs are reversed from the prior orbit behavior so the room now follows a natural grab-style camera gesture. Direct drag rendering still uses the existing responsive camera path and its 60 fps interaction allowance.

The room exposure and ambient, moon, desk, screen, lounge, and rack practicals are reduced for a deeper nighttime atmosphere. The former multicolor desk segments are replaced by one continuous blue emissive strip with two matching short-range blue light pools, keeping the desk edge visually unbroken while producing stronger local spill. Work lights retain a brighter optional state.

The former atom-like wall diagrams are replaced by labeled network-segmentation graphics with edge, identity, endpoint, server, and firewall tiers, plus an incident-response runbook board with a shield mark and response checklist. A rendered 1280×720 inspection confirmed the darker overview, continuous blue strip, tiled floor, locator visibility, and distinct wall zones. The page recorded a 105 ms local scene boot, zero horizontal or vertical overflow, and no browser warnings or errors. The regenerated recovery GLB contains 491 mesh nodes using 256 shared definitions and 19 materials at 1,165,876 bytes. Static validation passes all eight destinations and ten HTML routes; no deployment or form submission was performed.

## Scene legibility, layout, and motion pass — 2026-09-21

Eight cyan ring-and-dot locators now identify the interactive scene destinations. They use one shared procedural sprite texture, pulse subtly after entry, brighten on hover, and are themselves raycast targets for the same object destinations. A rendered click on the workstation locator opened the correct `FORENSIC WORKSTATION` camera state and `Projects built for real practice.` dossier.

The desk is wider and its portable objects have separate physical footprints. Automated Three.js bounding-box checks returned `false` for internship laptop/coffee mug, internship laptop/notebook, notebook/repository terminal, window/skills board, skills board/clock, and credential/wall-display intersections. The topology diagram, credential, evidence displays, network display, board, window, and clock now occupy distinct wall zones. RGB segments run along the desk edge with restrained magenta, violet, cyan, and green emission plus two short-range under-desk light pools.

The floor now uses a repeating tiled DataTexture on one continuous shell rather than 88 separate seam meshes. A modeled Roomba follows a slow collision-free foreground route. Direct pointer drag replaces the previous repeatedly restarted 180 ms camera tween, active camera rendering may reach 60 fps, wheel dolly is shortened to 140 ms, and idle ambient rendering remains capped at 30 fps. The post-change local boot observation was 109 ms. The regenerated recovery GLB contains 493 scene mesh nodes using 252 shared definitions and 22 materials at 1,165,948 bytes. Static checks pass for all eight destinations and all ten HTML routes; no deployment or form submission was performed.

## Startup optimization and material-depth pass — 2026-09-21

The blocking full-screen loader was replaced with a small non-interactive status indicator over the immediately usable poster and entry controls. A four-second guard dismisses it even if module fetching or WebGL initialization stalls. Critical local fonts and scene modules are preloaded. The browser still builds the room procedurally; the GLB remains a recovery/editing asset and is not downloaded by the homepage.

Repeated boxes, cylinders, spheres, and rounded forms now reuse geometry buffers. The exported recovery model still contains 565 scene mesh nodes but needs only 245 shared mesh definitions; its size fell from 1,812,596 to 1,136,648 bytes. Runtime material cloning is limited to interactive and animated objects instead of every mesh. The DPR cap is 1.25, the shadow map is 512 px, and only substantial forms cast shadows. A cold local render recorded `data-scene-boot-ms="120"`; this is a local browser observation, not a production-network benchmark. The entry and room were already visible and interactive when inspected.

The supplied workshop images informed a second depth pass: material-specific roughness/metalness, a dark night window with moon, stars, and city silhouettes, lower ambient fill, stronger warm/cool practical pools, brighter emissive screens, and more selective contact shadows. The result keeps the authored low-poly language while producing clearer depth and a more inhabited nighttime-room impression. The reference page exposed Three.js, OrbitControls, one external logo, and no downloaded 3D model in the observed state; that supports the same procedural/no-heavy-model strategy, though its server/cache conditions are not directly comparable to localhost.

## Continuous-room immersion correction — 2026-09-21

The homepage is now a fixed, full-viewport Three.js application rather than a conventional page with a 3D hero. Desktop document scrolling is disabled; orbit, dolly, raycasting, camera moves, ambient equipment activity, and object-led navigation all happen inside the room. The conventional document portfolio remains available as the separate `classic.html` route.

The former raised-floor diorama was replaced with a continuous interior shell. Its floor and three structural walls extend far beyond every permitted overview camera position, and the floor seams continue behind the camera into scene fog. The opening camera was moved closer and lower. Rendered checks at the opening view and an off-axis orbit confirmed that no floor edge, wall edge, or back of the set enters the viewport; the architecture now exits the frame and reads as an occupied room.

The authored scene exports as 565 finite mesh nodes across 18 materials. Eight real mesh groups are navigation destinations: forensic workstation, evidence drive, CCDC rack, internship laptop, field notebook, credential file, repository terminal, and secure-contact phone. A direct rendered click on the workstation moved the perspective camera and opened the correct `01 / 08` project dossier. The scene also includes the lounge, technical library, evidence locker, equipment cart, open field kit, wall displays, practical lights, animated screen activity, server and desk fans, and room-wide light controls.

`python scripts/check-portfolio.py` passes with all eight destinations, the regenerated shared-geometry GLB, ten HTML routes, local references, unique IDs, headings, image metadata, CSS assets, JavaScript syntax, and non-home guards. Desktop overflow remains locked; mobile/coarse-pointer visitors receive the static, link-complete fallback; reduced motion skips the entry animation and ambient motion. No deployment or live contact submission was performed.

## Environment-first lab evolution — 2026-09-21

The homepage opening is now a full-viewport desktop lab instead of a text column beside a contained Three.js figure. A brief skippable entry offers both Enter the lab and View case index. Once entered, four object labels are projected over the authored scene, object selection reframes the camera, and a readable right-side inspection dossier opens without navigating away. Escape, the close control, and Full room restore the overview. The original long-form content remains immediately below as the classic case index and retains every existing destination.

The scene grew from 228 to 271 validated meshes. New authored geometry includes the Pelycon internship laptop, network switch and port activity, a hard evidence case, campus backpack, headphones, Ethernet cable, and pinned topology diagram. These are contextual props, not fabricated portfolio claims. The CCDC rack, field notebook, forensic workstation, and internship laptop are the four primary targets. A low-frequency ambient pass affects only instrument illumination while the entered scene is visible; reduced motion disables it and the guided tour.

Rendered checks used local Microsoft Edge at 1440×900 and 390×844. Desktop observations covered the entry, full workspace, projected labels, CCDC camera focus, dossier hierarchy, close behavior, and horizontal overflow. Mobile observations confirmed no page overflow, a conventional introduction, static workspace composition, four visible direct destination links, no projected hotspot/tour controls, and no autoplay. A 1440×900 reduced-motion run confirmed the entry gate is absent, tour state is false, and the page has no horizontal overflow. No console errors were observed in the completed mobile run. `python scripts/check-portfolio.py` passes with 271 finite meshes, four real destinations, regenerated GLB structure, nine HTML routes, 175 local references, unique IDs, headings, image metadata, CSS assets, and JavaScript syntax. No deployment or form submission was performed.

## Autoplay, GitHub count, and experience hierarchy — 2026-09-21

The workstation tour now starts automatically 1.8 seconds after the WebGL scene becomes ready and advances every 6.5 seconds. It remains visibly pausable, suspends its timer in hidden tabs, and does not autoplay when `prefers-reduced-motion: reduce` is active. Rendered inspection observed the selected object, camera framing, inspector copy, pressed state, progress, and destination advancing while the visitor was reading another section.

GitHub's public API returned seven public repositories for `TateWilson1`: TabletopForge, Security-Toolkit, GRC-Governance-Risk-and-Compliance-, testing_password_strength_with_GUI, TateWilson1.github.io, TateWilson1, and Vulnerability_Scanner. The homepage archive is not that same set—it includes CFReDS and omits the retired scanner—so the misleading “7 projects” claim was replaced with the neutral “Project archive.”

The detached hero CCDC proof strip was removed. The first-place Kentucky result remains prominent where it has context: the enlarged rack trophy, the CCDC scene stop, and the dedicated CCDC record. Internship experience is now a separate bordered professional record with Pelycon Technologies, role, dates, and three responsibilities visible without expanding a disclosure. Desktop rendering, semantic structure, automatic tour advancement, JavaScript syntax, 228 scene meshes, four real destinations, nine routes, and 176 local references were checked. No deployment or form submission was performed.

## Guided workspace and CFReDS update — 2026-09-21

The workspace has a four-stop guided sequence: monitor, notebook, server rack, and evidence drive. Previous/next controls wrap through the sequence; the later autoplay pass starts it after a short delay and advances every 6.5 seconds. Pause, Escape, Full workspace, manual object selection, or manual rotation stops the automatic sequence. Hidden tabs cancel the pending timer and resume only if the tour was left playing. Camera position, look target, zoom, object emphasis, inspector copy, destination, pressed state, and `01 / 04` progress update together. Reduced motion uses the same content and controls without autoplay and with immediate camera state.

Rendered checks covered the overview, monitor framing, notebook selection, CCDC rack framing, enlarged trophy/plaque, evidence-drive state, sequential previous/next behavior, and play/pause advancement. The CCDC result appears in the rack inspector, enlarged rack-top object, and background record as first place in Kentucky; the later hierarchy pass removed it from the hero. The homepage's second featured project is now the NIST CFReDS Hacking Case using only the user-supplied scope and tool list. It is labeled “Case study page in progress,” has no fabricated repository link, and replaces the Windows scanner in the homepage archive and primary case navigation. The old scanner page remains on disk but is no longer a primary destination.

## Workspace refinement — 2026-09-21

The homepage now uses an original Three.js current-workspace scene in the permanent source. Four primary objects map to projects, internship, education, and CCDC. Selecting one reframes the camera, updates an HTML inspector, and leaves navigation as an explicit link. Full workspace and Escape clear selection. Direct links and the authored SVG poster remain as the no-script/WebGL fallback.

Rendered desktop checks covered the default composition, evidence-drive focus, selected-work section, expanded seven-project archive, background records, and the TabletopForge case header. The scene uses no idle animation loop; frames are scheduled only for load/resize/interaction and finite camera settling. The DPR cap, visibility observers, WebGL failure fallback, and reduced-motion immediate-state branch remain. A narrow no-script smoke capture exposed mobile navigation wrapping and a collapsed line break; the CSS now uses a two-column no-script navigation fallback and preserves the deliberate hero line break. The headless browser enforced a wider layout viewport than its requested screenshot width, so that capture is not claimed as proof of exact 390px reflow.

Seven selected portfolio entries are discoverable through a native archive disclosure, and all three field notes are linked from the homepage. `python scripts/check-portfolio.py` passes with finite meshes, four real destinations, matching exported GLB structure, nine HTML routes, local references, unique IDs, heading checks, image metadata, CSS asset checks, and JavaScript syntax. The GLB recovery/export asset was regenerated from the authored scene model. No deployment or form submission was performed.

Verified locally on 2026-09-21 against `C:\Users\Steve\Documents\Portfolio Website`. Not published.

## Delivered system

The homepage and six case-study / field-note pages share the evidence-workbench system in DESIGN.md: graphite surfaces, pale-blue actions, warm evidence annotations, Barlow headings/body text, and IBM Plex Mono metadata. Selected work is a vertical editorial sequence. The four-mode conceptual evidence map is the signature interaction. It distinguishes digital-forensics study from completed project evidence.

All seven archive entries remain. Substantive article text across all six reading pages matches the pre-edit backup after stripping markup. Resume PDF/DOCX, contact destinations, native Formspree action, and legacy projects/resume redirects remain. Existing biography and credentials were preserved, not independently verified. The unrelated Python application was outside scope.

## Rendered review and iteration

- All seven content pages checked for page overflow at 375, 430, 768, 1024, 1440, and 1920 CSS pixels. Additional 320px reflow check passed.
- Fixed a narrow-screen map width defect and an intermediate-width GRC field-note diagram overflow, then reran affected widths. Final 320px map labels and two-column buttons were inspected visually.
- Reviewed desktop hero, selected projects, case header and exercise diagram; tablet notes; mobile map, contact and risk matrix. Actual scanner output image loaded successfully.
- Taste audit passes removed competing navigation, scroll-driven project presentation, tiny diagram labels, repeated card framing, and inconsistent palette choices. Final review retained the restrained geometry and real project-specific figures rather than adding decorative cyber effects.
- Scoped Impeccable polish passes corrected case-header proportions, metadata alignment, responsive diagram layout, label wrapping, and keyboard navigation focus. This was a source/rendered review using the pinned reference, not the full Impeccable CLI/detector or multi-agent critique process.

## Interaction and accessibility checks

- All four map modes activated with Enter. One mode is pressed at a time; the inspector and destination change together. URL state and browser Back restore selection.
- Filters return All 7 / Cybersecurity 5 / GRC 2 / Web 2. Reload retains the selected category.
- Mobile navigation opens, closes after choosing a link, and closes on Escape with focus returned to Menu.
- Skip link focuses main. The dialog supports empty search, ArrowDown, Enter, and Escape. Escape returns focus to its opener. Choosing a same-page result focuses the destination section; the next Tab from Contact reaches the email link with a visible focus outline.
- The named risk-matrix region accepts keyboard focus and ArrowRight scrolls its internal 680px table within a 318px region; the page itself does not overflow.
- Form name, email and message retain required-field constraints and labels. Read-only validity inspection confirmed empty values are invalid. No form submission or message delivery was attempted.
- With scripts removed by the preview's `?nojs=1` mode: navigation remains visible, all seven projects remain, inactive filters/map controls are hidden, and the useful default map explanation remains. No page overflow at 375px.
- No browser console warnings or errors were observed during final checked interactions.

Computed browser palette values matched CSS tokens. Calculated contrast:

| Pair | Ratio |
| --- | ---: |
| Main text / canvas | 16.28:1 |
| Muted text / panel | 8.46:1 |
| Subtle metadata / raised panel | 5.57:1 |
| Blue links / panel | 10.37:1 |
| Warm annotation / panel | 8.56:1 |
| Control border / section surface | 4.86:1 |

These checked text pairs exceed 4.5:1 and the checked control border exceeds 3:1. Decorative rules are not control boundaries. Selected map state also has text and pressed-state feedback.

Motion source review: a 260ms connector trace runs only with `prefers-reduced-motion: no-preference`; reduced motion disables smooth scrolling and component transitions. No animation loop or content-reveal delay exists. Runtime OS reduced-motion emulation and 200% browser zoom were not available in this review; 320px reflow was checked directly. This is not a full screen-reader or cross-browser certification.

## Engineering and performance

`python scripts/check-portfolio.py` passes: nine HTML routes, 171 local references, unique IDs, one H1 per content page, main targets, image descriptions/dimensions, CSS asset existence, and JavaScript syntax. The behavioral scene test covers mode selection, destinations, URL state, invalid modes, history and non-home pages. These are focused static/behavior checks, not a standards-complete HTML validator.

| Asset | Final bytes, uncompressed |
| --- | ---: |
| styles.css | 23,907 |
| case-studies.css | 34,618 |
| script.js | 9,494 |
| scene.js | 2,930 |

The map no longer imports the 808,647 bytes of Three.js vendor modules. They and their license remain unused for recovery. Six Latin WOFF2 files totaling 119,360 bytes are self-hosted with OFL licenses; fonts no longer depend on Google at runtime. No framework, package manager, graphics dependency, or build step was introduced. These are file-size observations, not Lighthouse or field-performance scores.

Project skill frontmatter and referenced files were validated with a standard-library fallback; the generic skill validator could not run because PyYAML was unavailable. No package was installed solely for that check.

## Preview, recovery, and remaining opportunities

Run `python scripts/preview-portfolio.py` and open http://127.0.0.1:8765/ . The allowlisted localhost server reads the permanent source directly, serves public frontend files/assets, and excludes the unrelated mixed-workspace files. `%TEMP%\tate-portfolio-preview` is not the delivered source and requires no synchronization.

Pre-edit frontend backup: `C:\Users\Steve\AppData\Local\Temp\tate-portfolio-before-20260920-225208`.

Next useful additions are an authentic, sanitized digital-forensics case study and real TabletopForge product screenshots when available. Before publication, check Safari/Firefox, a screen reader, real mobile hardware, OS reduced motion, zoom, and the contact provider through an explicitly authorized test. The existing scanner image remains externally hosted; consider a licensed local copy for resilience. External repositories were preserved but not exhaustively crawled.

No deployment or live contact submission was performed. All implementation, fonts, project-local skill references, design documentation, and reusable checks are saved in the permanent Portfolio Website folder.

## Spatial route refinement — 2026-09-22

- Compared the live Avagianos workshop and the local portfolio in the same browser session. The local direction keeps the reference's full-room canvas, projected object controls, ambient equipment motion, and contextual side panel while retaining Tate's quieter evidence-led palette and destination-specific camera framing.
- Added explicit `ENTRY`, `HOME`, and eight dossier states to the room runtime. Each dossier now owns a stable URL hash, and loading `index.html#projects` was observed entering the room, framing the workstation, and opening the correct panel without showing the entry gate.
- Exercised all eight projected controls in the browser. Each produced the expected hash, named state, panel title, open modal state, and inert scene navigation. Explicit close returned to `HOME`; browser Back from `#projects` returned to the overview with the panel `aria-hidden="true"`.
- Clicked the main monitor mesh itself and observed the `PROJECTS` state and project dossier, confirming the raycast target rather than only the projected HTML control. A drag changed the projected workstation control's x-position by about 50 CSS pixels, and a wheel gesture changed its y-position by about 81 CSS pixels, confirming the orbit and dolly paths were active.
- The final overview reported a 1336×990 renderer buffer at the current desktop viewport, a 245 ms observed scene boot for that run, and no captured browser console warnings or errors.
- The browser's responsive viewport override did not change its reported layout viewport in this session, so the existing 390px mobile render evidence was not falsely re-claimed as a new runtime check. The mobile media-query fallback and its six direct destinations remain present in source. No external portfolio link, résumé download, mail link, or contact form was activated.

## Object and material rebuild — 2026-09-22

The new object-quality brief was audited against the live local overview and focused workstation, evidence, and rack views. The per-object inventory and keep/rebuild decisions are in `work/scene-asset-audit.md`. A fresh browser comparison with the Avagianos workshop informed silhouette, material separation, and staging; no benchmark model or texture was copied.

The desk is now a layered veneer/laminate assembly with rails, tubular frame, grommet, and drawer pulls. The main monitor has an inset display, separate bezel, rear mount, vents, stand, and a factual canvas-rendered project interface. Keyboard keys, mouse, laptop, repository terminal, 2.5-inch evidence drive, forensic cart, write blocker, and network ports received distinct physical details. The chair uses contoured woven panels, armrests, shell, casters, and a five-spoke base. The rack is freestanding beside the desk with open rails, patch/switch/server/UPS faceplates, RJ45 sockets, vents, drive bay, status lights, and curved patch leads. A spun steel task lamp and cushion-built lounge sofa replace two conspicuously blocky secondary silhouettes. The decorative trophy, beanbag, neon lock, and neon shield were removed.

Browser review at the available 1069×792 desktop viewport showed a clearer equipment hierarchy and more readable screen content than the before view. The former rack side view was corrected so its front hardware is visible with the dossier open; the evidence drive is likewise inside the visible scene region. Five changed object destinations (`projects`, `forensics`, `ccdc`, `internship`, `github`) were opened and closed in the browser with correct hashes and panel states. The final local reload reported a 293 ms scene boot and no captured console warnings/errors. The viewport-height document remained 792px with no desktop scroll.

`node scripts/export-workstation.mjs` and `python scripts/check-portfolio.py` pass. The recovery GLB is 1,847,948 bytes, 765 finite mesh nodes, 275 shared mesh definitions, and 32 materials; the geometry increase buys visible detail but has not been field-profiled on a recruiter laptop. Runtime screen art and procedural wood/fabric maps are generated in the browser, not baked into the recovery GLB. No deployment or contact submission occurred.

## Left-wall spatial revision — 2026-09-23

- Reorganized destinations 07, 02, and 05 as three distinct room zones instead of a camera-facing stack: the repository terminal now faces inward from the rear left wall, the forensic bench turns perpendicular to the camera with its equipment cart beside it, and the notebook sits at an angle beside the lounge.
- Reduced the DFIR neon fixture to 72% of its former scale and lowered its scene-light intensity so the credential wall and room furnishings retain visual hierarchy.
- Rebuilt the framed credential from the supplied CompTIA Security+ certificate reference. The room version preserves its red CompTIA header, Security+ circular badge, gray seal, credential-date column, signature line, and letter-size proportion while intentionally omitting the candidate ID and verification code from the public texture.
- Updated all three object targets, camera positions, and projected markers after the move. Focused browser checks confirmed the forensic drive, repository terminal, lounge notebook, and certificate remain visible beside their dossier panels.
- `node --check scene.js`, `node --check scene-model.mjs`, `node scripts/export-workstation.mjs`, and `node scene.contract-check.mjs` pass. The regenerated recovery GLB contains 998 finite mesh nodes, 424 shared definitions, and 64 materials. The live overview had no document overflow and produced no captured browser warnings or errors. No deployment or contact submission occurred.

## Full-room polish pass — 2026-09-23

- Rechecked the complete overview after the left-wall revision. The dominant desk remains centered, the lounge and aquarium hold the two corners, and all eight projected controls remain separated and attached to their physical destinations.
- Consolidated practical-light color roles: cyan identifies technical work, warm amber identifies the lounge and human work light, and pink is reserved for the verification sign. The library now uses cyan, blue, and warm-white shelf light instead of the previous cyan/violet/green rainbow; the forensic and contact accent strips now join the cyan system.
- Increased the skyline texture's emissive response and changed the frame and mullion to brushed steel so the requested panoramic window reads as architecture behind the displays. Raised default exposure from 1.04 to 1.07 and ambient fill from .075 to .095, while keeping both side-wall fills below .25. Reduced lounge, rack, shelf, and aquarium hotspots to preserve material detail.
- Visually checked the default room, brighter work-light mode, and Security+ credential close-up at the available 1068×790 viewport. The default still reads as a night lab; work-light mode brightens the floor and corner furnishings without clipping the practicals; the framed credential remains legible and fully visible beside its dossier.
- The live room reported a 255 ms scene boot, no viewport overflow, and no captured browser warnings or errors. `node --check scene.js`, `node --check scene-model.mjs`, `node scripts/export-workstation.mjs`, `node scene.contract-check.mjs`, and `python scripts/check-portfolio.py` pass. The prioritized status checklist is saved in `ROOM-POLISH-CHECKLIST.md`. No deployment or contact submission occurred.

## Production-upgrade pass — 2026-09-23

- Cloned and ran Tate's public TabletopForge repository locally on port 3210. Its repository contained only a screenshot placeholder, so the portfolio now uses two authentic 1440×900 captures from the working application: the homepage and the exercise-generator form. No placeholder or generated marketing mockup was promoted as evidence.
- The real homepage now appears on the room's main workstation. The projects dossier contains the real generator image, and the TabletopForge case study presents both images with factual captions and explicit dimensions. Both assets loaded at their full 1440px natural width with no browser warnings or horizontal overflow in the checked desktop and 390px gallery layouts.
- Added an automatic reduced rendering profile for systems reporting no more than 4 GB device memory or 4 logical processors. It caps pixel ratio at 1, reduces the two primary shadow maps to 512px, shrinks bloom buffers, disables antialiasing, and lowers active/idle frame cadence. `?quality=low` forces the same profile for repeatable QA; the forced profile rendered the complete room at 1280×720, reported the expected `reduced` state, booted in 485 ms, and produced no console warnings or errors.
- Rechecked the current résumé source: expected graduation remains December 2026; Pelycon is dated May–August 2026; GPA is 3.60; CCDC leadership, Security+, and ISC2 CC remain listed. The supplied Security+ certificate records May 18, 2026 through May 18, 2029. The current portfolio claims inspected in this pass remain consistent with those supplied sources.
- The available browser inventory contains only the Chromium-based Codex in-app browser, and the Windows host exposes no native app/screen-reader surface to this task. A true Safari, Firefox, real-screen-reader, 200% browser-zoom, and physical-phone pass therefore remains an explicit predeployment gate rather than a falsely claimed check.
